{"$schema": "https://json-schema.org/draft/2020-12/schema", "title": "artifactbench.role_canonicalization", "description": "01_evidence/rtti/role_canonicalization.json — classes whose frozen Static Recovery v2 label (PROTECTED_SUBSYSTEM) was translated to the current LICENSING_AND_ENTITLEMENT_SUBSYSTEM before any AB report, instruction or export used it (ADDENDUM C2). The original role is kept for provenance.", "type": "object", "required": ["rule", "v2_label", "canonical_label", "classes", "rewritten_files"], "properties": {"classes": {"type": "array", "items": {"type": "object", "required": ["class", "original_role", "canonical_role"]}}, "rewritten_files": {"type": "array", "items": {"type": "string"}}}}
