{"$schema": "https://json-schema.org/draft/2020-12/schema", "title": "artifactbench.stages", "description": "evidence/00_manifest/stages.json in an export — every stage record of the job at export time, so an import restores the identical state (ADDENDUM B7 round-trip).", "type": "object", "required": ["job_id", "stages"], "properties": {"job_id": {"type": "string"}, "stages": {"type": "array", "items": {"type": "object", "required": ["job_id", "stage", "status"]}}}}
