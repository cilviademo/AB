{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "artifactbench.hashes",
  "type": "object",
  "required": ["algorithm", "files"],
  "properties": {
    "algorithm": {"const": "sha256"},
    "files": {"type": "array", "items": {"type": "object", "required": ["path", "sha256", "size"]}}
  }
}
