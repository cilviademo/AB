# STATUS — what has been run, where

Every gate below is either **measured** (run here, output quoted in the commit),
**pending-windows** (scripted, needs the studio PC) or **blocked** (see
BLOCKERS.md). Nothing is marked green without a run.

| Step | Gate | State |
|---|---|---|
| 1.1 | `ab-cli doctor` reports shell, engine, disk, tools | measured (Linux): 11 pass · 1 warning · 4 unavailable · 0 fail |
| 1.1 | `AB.exe` launches from a clean ZIP with no dev tools | pending-windows |
| 1.2 | no-change re-run does zero work; DECOMPILATION version bump re-runs only it and later | measured: engine/tests/test_jobs.py (24 tests green) |
| 1.3 | 4 fixtures + 3 presets → 4 jobs, correct attachments; 500 MB under 400 MB RSS | measured (synthetic PEs): 4 jobs, attachments by prefix, pdb to all; 513 MB corpus → peak RSS 39 MB, 4.0 s |
| 1.4 | `diff-baseline` zero evidence-status diffs, ≤ 20 % timing variance | measured on the synthetic fixture (see commit); the four corpus fixtures are blocked (B-001): descriptors in place, `ab-cli diff-baseline --init <name>` once the binaries are dropped |
| 1.5 | invalid-path and secret scanners pass on the four fixtures | measured on the synthetic fixture export (GIT_READY true, build/validation rows pending); corpus fixtures blocked (B-001). The secret scanner caught a real leak (absolute tool paths in tool_versions.json) which was fixed |
| 1.6 | stripped fixture → static bundle; GROUND_TRUTH_REPORT thresholds | measured (Linux stripped ELF build): RTTI 5/5, resources 3/3 VALID_EXACT, BinaryData names 3/3, <PARAM> keys 7/7, 0 illegal paths, 0 keys promoted. MSVC/PE variant: pending-windows (`fixtures/groundtruth/build_all.ps1`) |
| 2.1 | factory/parameters/units/buses/info/state match truth.json | measured (Linux): identity vendor+product VERIFIED_RUNTIME, codes Mbnd/Abgt from the JUCE FUID, 7/7 parameters with defaults/steps/units/types |
| 2.2 | 100 % params mapped with representation; decoy classified from runtime | measured (Linux): see commit; JUCE wrapper bypass is RUNTIME_ONLY by design |
| 2.3 | FIDELITY build configures with recovered identity | identity.cmake written from VERIFIED_RUNTIME facts; configure run pending (needs JUCE in the export, Phase 4 build worker) |
| CI-5 | vst3host fuzz: 20 hostile inputs → codes 2–5 | measured: engine/tests/test_runtime_host.py |
| 4.1 | probes at 44.1/48/96 kHz × blocks 32…1024, one parameter at a time; measurements.json | measured (Linux, fixture): 79 renders, 0 failures; ramp probe carries a settle lead-in (D-017) after the fits exposed the original's 20 ms parameter smoothing |
| 4.2 | fit by residual; evidence_source/ + human_source/; gates in code | measured: default ramp → `tanh_normalized` (RMSE 6.85e-5, tie with `tanh` broken by law simplicity); laws recovered one-at-a-time: inputGain dB→pre, outputGain dB→post, drive ∝ plain value, Mode Clean=linear 0.5012 / Hot=×2, bypass=passthrough, oversample=no static effect, cutoff=unmodeled (filter); Active gets the module only at ≤ 1e-4 RMSE; FIDELITY refused without VERIFIED_RUNTIME identity (test) |
| 4.3 | SURROGATE build + pluginval ≥ 5 + differential harness | measured (Linux, g++/Ninja/JUCE 8.0.9): BUILT in 120 s, 0 errors; pluginval 1.0.4 strictness 5 PASSED (16 tests, GUI skipped); state cross-load CROSS_LOAD_VALIDATED both ways (7/7 params) |
| 4 exit | fixture waveshaper BEHAVIORALLY_EQUIVALENT (RMSE ≤ 1e-4, spectrum ≤ 0.1 dB) with build + pluginval green | measured: Waveshaper (default ramp) BEHAVIORALLY_EQUIVALENT — RMSE 6.85e-5, Δspectrum 0.0002 dB; sweeps PERCEPTUALLY_CLOSE (worst 3.4e-4 at Hot/+24 dB drive: fractional-sample latency of the original's oversampling FIR, recorded as a TODO); filter / oversampling / whole plugin FAILED as expected (scaffolds, not compiled); MSVC variant pending-windows; owned SP plugin blocked (B-003) |
| 1.6 (re-measured, D-022) | stripped fixture → static bundle; GROUND_TRUTH_REPORT phase-1 thresholds | measured on the **truly stripped** ELF (`.symtab` 0, `-fvisibility=hidden`): RTTI 5/5 — 0 from v2 static (no `_ZTS` symbols), 5 as `CANDIDATE_TYPEINFO_STRING` from the AB typeinfo-name scan; resources 3/3, BinaryData names 3/3, `<PARAM>` keys 7/7, 0 illegal paths, 0 keys promoted. The earlier 1.6 row measured a build that still carried its symbol table |
| 2.1–2.2 (re-measured) | runtime + state-differential on the stripped build | measured: identity/params/state gates all PASS on the stripped build (phase 2 OK); SDK validator 47/47 PASSED as the second validation source |
| 3.1 | first-run downloader (JDK 21, Ghidra, pluginval, JUCE) with pinned SHA-256; offline → SKIPPED with reason | measured (Linux): `ab-cli tools install` fetches Temurin 21.0.8+9 / Ghidra 11.3.2 / pluginval 1.0.4 / JUCE 8.0.9 against tools/manifest.json hashes (symlinks in tarballs skipped); missing tool → DECOMPILE / BUILD SKIPPED with the install hint |
| 3.2 | fixture: processBlock, prepareToPlay, state functions located; waveshaper + filter in the top-5 DSP candidates; fingerprints stable Release+PDB ↔ stripped | measured on the **truly stripped** build (no symbols): ExportRTTI structural Itanium pass recovers 709 classes / 592 vtables incl. ABGroundTruthAudioProcessor (76 slots, base juce::AudioProcessor); the knowledge base, taught by one symbol build (692 vtable layouts), seeds processBlock 0x235280 / prepareToPlay / releaseResources / get+setStateInformation / createEditor from the layout (75/75 slot fingerprints match, basis CANDIDATE) → gate PASS; DSP top-5: re-measured after the Fingerprint size fix (see commit); fingerprint stability: A3 row (100 % release↔stripped by norm hash + CFG on 13 963 functions) |
| 3.3 | fixture PNG and TTF resolve to their original filenames | measured on the stripped build: getNamedResource decompiled, 3/3 pairs named by the JUCE name-hash case constant, bytes sha256-equal to Resources/knob.png (790 B), ABMono.ttf (2288 B), Presets/Init.xml (360 B) → VERIFIED |
| 3.4 exit | re-analysis after cache seeding skips ≥ 80 % of JUCE functions; a modified DSP constant is not matched as identical | measured (stripped job after the symbol job taught the cache): known 98.3 % / near_match 1.7 % / unknown 0 % of 13 963 functions, 8 092 KNOWN_FRAMEWORK + 524 KNOWN_THIRD_PARTY matches suppress deep work (8 474 / 13 963); modified-constant → near_match + history downgrade at unit level (A2 row); fixture-level modified build pending |
| C1 | no code path branches on ownership; every report carries usage_context / source_availability; identical stage execution per context | measured: engine/tests/test_bundle.py::test_context_changes_wording_never_capability — USER_RECOVERY / BLACK_BOX_REFERENCE / KNOWN_SOURCE_FIXTURE / legacy THIRD_PARTY export the identical file set; only CONTEXT.md and the manifest interpretation differ; `reconstruction_allowed` removed |
| C2 | licensing stub (serial check + demo state) recovered with the DSP's metrics; a bypass edit reported TRANSFORMED_BREAKING | measured (unit): faithful reconstruction not a bypass; `return true` / `licensed = true` → TRANSFORMED_BREAKING; no evidence → LICENSE_REQUIRES_MANUAL_REVIEW. **Fixture-level (stripped build, job `ab-0a2a655ac977`): measured** — `abgt::LicenseStub` found by the structural RTTI pass, role LICENSING_AND_ENTITLEMENT_SUBSYSTEM (CANDIDATE, name tokens; ground-truth metric `licensing_subsystem_classified` true), scaffolded to `RecoveredScaffolds/Licensing/abgt__LicenseStub.h` (evidence-only header: vtable 0xa39e50, 2 method addresses, no invented members), licence state fields `demoMode` / `serialChecksum` recovered from the runtime state and CROSS_LOAD_VALIDATED both ways, 0 bypass findings, state LICENSE_REQUIRES_MANUAL_REVIEW (check implementation not reconstructed — honest). Fix on the way: on stripped builds the reconstruction only scaffolded static-RTTI classes, so no class scaffold existed at all (RECONSTRUCT v5, VALIDATION v5) |
| Naming | SSL-style vendor names never gate recovery; reversible identifier_map; ids/state keys never renamed silently | measured (unit + fixture job): SSLComp→Compressor, SSLBusComp→BusCompressor, SSL1176→RecoveredClass_NNNN, SSLDrive param id untouched (display "Drive"), state key migration PROPOSED only; fixture (CANONICALIZE): ABGroundTruthAudioProcessor→AudioProcessor, abgt::* untouched, decompiler labels never mapped |
| C3 | MODERNIZE builds and stays ≥ BEHAVIORALLY_EQUIVALENT for DSP with every intentional change listed; PRESERVE ORIGINAL yields no transformation nodes | measured (unit, synthetic project): PRESERVE_ORIGINAL → 0 nodes, Active = recovered; MODERNIZE → transformed_source + Active from it, graph nodes, CANONICALIZE naming; MIGRATE → NOT_AVAILABLE recorded. **Fixture-level (stripped job, goal MODERNIZE, preserve_dsp_behavior YES): PASS** — transformed_source/DSP/Saturation.h (ab_transformed::SaturationStage, juce::dsp block API) builds (SURROGATE BUILT, pluginval strictness 5 green); ORIGINAL↔TRANSFORMED 79 renders, Waveshaper BEHAVIORALLY_EQUIVALENT (worst RMSE 6.85e-05, Δspectrum 2.2e-04 dB); RECOVERED↔TRANSFORMED BIT_EXACT (79/79, RMSE 0); graph: active TRANSFORMED, validated TRANSFORMED, statuses MODERNIZED_EQUIVALENT 1 / TRANSFORMED_COMPATIBLE 1 / RECOVERED_EXACT 1, intentional_behavioral_changes [] (none intended, none measured); state CROSS_LOAD_VALIDATED. Note: RECONSTRUCT stage version 3 (the cache reused v2 output after the generator fix; a generator change is a stage-version change) |
| A1 | doctor lists every dependency with version, licence, pinned hash; LIEF inventory reproduces v2 `pe` | measured: `dep:*` rows for lief/capstone/tlsh/yara/numpy/jsonschema + `tool:*` rows incl. validator and juce; `v2_pe_crosscheck` MATCH on the synthetic PE fixture (exports walked v2-style: LIEF refuses the hand-made table) and on the fixture ELF; corpus fixtures blocked (B-001) |
| A2 | second run ≥ 80 % KNOWN_*; modified constant → near_match + history downgrade | measured (unit level + synthetic fixture): 100 % known on the second pass; a constant edit on an identical normalized stream → `near_match`, `classification_history` row "contradiction … CONSTANT_SIGNATURE differ"; IMPLEMENTATION_VERIFIED only with ≥ 2 binaries and a behaviour confirmation (tested). Fixture-level delta on the stripped build: pending the current Ghidra run |
| A3 | ≥ 90 % of DSP functions match across builds by norm hash or CFG+constants | measured: Release+symbols vs Release-stripped (same flags): 13 963 / 13 963 functions ≥ 32 B matched on normalized hash + CFG (100 %), 12 780 raw-equal; all 7 fixture DSP/processor functions matched (norm 7/7, CFG+constants 6/7). Debug (-O0) vs stripped: 5/14 named DSP functions (different optimisation level — reported, not claimed). Two earlier 0 % results were measurement bugs (non-allocated .debug sections inflating the address range; visibility flags differing between variants), both fixed |
| A4 | four fixtures → expected candidate families; JUCE rule silent on iPlug2 | measured: fixture ELF → AB.Framework.JUCE + AB.Framework.VST3SDK, no Gen rule; synthetic PE → JUCE + SoundTouch markers (its strings); corpus fixtures blocked (B-001); iPlug2 check pending-windows (B-008) |
| A5 | one drop of every artifact type → identified or PRESERVED_UNPARSED; both fixtures' reports | measured: unknown types listed as PRESERVED_UNPARSED in input_manifest.json (kept in the object store); iPlug2 fixture source written, unbuilt here (B-008) |
| A6 | project log shows the fixed commit sequence; git status clean; builds from a fresh clone | measured (unit): sequence ingest:/recovery:/reconstruction:/validation: with large binaries ignored, status clean. **Fixture-level (fresh full run, rebuilt stripped fixture, job `ab-0a2a655ac977`, 1 075 s end-to-end):** log = ingest → recovery: static evidence → recovery: runtime metadata → recovery: RTTI and callgraph → recovery: reference behaviour → reconstruction: implement Waveshaper → build: validated VST3 reconstruction → validation: Waveshaper matched original (BEHAVIORALLY_EQUIVALENT, rmse=6.85e-05) → validation: agent handoff regenerated; every phase 1–4 gate PASS, 0 FP / 0 FN (P2 11/11 state fields incl. demoMode/serialChecksum); the evaluator's three report files were untracked after the run (fixed: the evaluator now checkpoints them) |
| A7 | exported fixture project builds without the app; knowledge_used.json explains every skip | measured: `<Plugin>_RECOVERED/` layout with evidence/, validation/, knowledge_used.json and the A7 GIT_READY rows (engine tests); fresh-clone build of the export: pending the current run |
| B1 | post-build audit before any new feature | measured: docs/hardening/POST_BUILD_AUDIT.md + reports/post_build_audit.json (every stage, worker, contract and export path audited; findings fixed in the B3/B4/B5 commits) |
| B2 | real build matrix | measured (Linux): docs/hardening/BUILD_VALIDATION_REPORT.md — app `npm run build`, engine `pytest`, native `cmake --build`, fixture Debug/Release/stripped, fresh-clone build 3 m 53 s; Windows/MSVC rows pending-windows |
| B3 | no false green states | measured: completion contracts per stage (`runner.CONTRACTS`), CONTRACT_UNMET fails the stage, missing tool → BLOCKED with a setup hint, never SKIPPED-as-OK (engine/tests/test_jobs.py) |
| B4 | universal artifact router | measured: `ab-cli route` — magic + extension + context identification, capability registry (one entry per type), UNKNOWN_ARTIFACT preserved as a full record, PDB GUID/age relationships, no merge on a file name (engine/tests/test_router.py, test_acceptance_mixed_drop.py) |
| B5 | known-source validation system | measured: 06_validation/known_source_metrics.json + KNOWN_SOURCE_VALIDATION_REPORT.md — separate recall/precision/mapping/behaviour metrics, failure classes, benchmark integrity (fixture source withheld from recovery) (engine/tests/test_groundtruth.py) |
| B6 | reference library and knowledge hardening | measured (fixture workspace): `ab-cli reference` → 4 USER_ARTIFACT · 1 KNOWN_SOURCE_FIXTURE (abgt-juce-1 resolved from fixtures/groundtruth/fixture.json: PROPRIETARY licence, Release/stripped build, source dir) · 5 FRAMEWORK_REFERENCE (KNOWN_FRAMEWORK 19 075 fns, KNOWN_THIRD_PARTY 1 544 fns, 3 shared implementations with tier) · 33 DSP_REFERENCE (MIT, per-file sha256, CANDIDATE); every entry carries origin / source artifact / licence / hash / analysis version / evidence state / verification date / relationships; `--provenance ABGroundTruthAudioProcessor` answers with the learning artifact, its type, occurrences, history; fixture-source isolation OK (4 projects × 7 fixture files, 0 byte-identical copies); `--dashboard` counts (7/7 params, 9/11 state, 5/5 classes, 3/3 entry points, 3/3 resources, 0 FP, RMSE 6.85e-5). Reference tab in the app with the four explicit actions; nothing inferred from a co-drop (engine/tests/test_reference.py, 6 tests) |
| B7 | matrices, robustness, portability | measured: PLUGIN/ARTIFACT compatibility matrices; adversarial drop (malformed PE, truncated VST3, unclosed XML, corrupt PNG, nested zip with traversal + absolute members, duplicate names, 240-char path, Unicode + spaces, Windows-reserved names, empty file) → inventoried, refused members never extracted, STATIC fails safely, export portable; router never raises on garbage; Unicode/spaced workspace + plugin name → export with 0 path/secret findings and no machine root; fresh machine: `AVAILABLE | MISSING | WRONG_VERSION | UNSUPPORTED` + guidance on every dep/tool/engine row, probe exceptions become rows; migrations: jobs.db v1 → v2 keeps rows (legacy OWNED read through the alias), knowledge.db v1 → v2 adds `tier` in place, newer schemas refused, contract major versions never reinterpreted; export round-trip: export (with `evidence/00_manifest/stages.json`) → `ab-cli import` on a second workspace → same job/context/stage records/cache keys, evidence tree byte-identical, re-export identical; checkpoints cover ingest/static/runtime/decompile/reconstruction/validation with NOTHING_TO_COMMIT on no change; docs/hardening/PERFORMANCE_REPORT.md (cold 1.33 s vs cache-hit 0.23 s on the synthetic PE, per-stage fixture timings, DECOMPILE 787 s dominated by Ghidra). 21-plugin corpus regression blocked (B-001) (engine/tests/test_hardening_b7.py, 9 tests) |
| B8 | acceptance tests | **1 · unseen fixture — measured (first blind run, recorded as-is):** `fixtures/groundtruth_unseen` (ABUnseen: RC one-pole + atan shaper, no oversampling, no licensing; written after the pipeline, never used to tune it), stripped binary only → full pipeline (job `ab-1045319e087c`, 967 s) → scored against its own truth.json: identity VERIFIED_RUNTIME, parameters 6/6 (recall and precision 1.0), state fields 8/8 incl. the decoy from runtime, classes 4/4 (typeinfo CANDIDATE), resources 2/2 VALID_EXACT, processBlock/prepareToPlay/state functions located (knowledge layout 57/57), waveshaper family **atan** recovered (RMSE 1.51e-5), SURROGATE built, pluginval 5, Waveshaper BEHAVIORALLY_EQUIVALENT (RMSE 1.51e-5, Δspectrum 3.3e-5 dB), CROSS_LOAD_VALIDATED, 0 FP / 0 FN, integrity ok (source never in the inputs). **One honest miss:** P3 "filter in top-5 DSP candidates" FAIL — `abus::OnePoleLP` was not attributed (failure class CLASSIFICATION 1): the name-token vocabulary knew *lowpass/filter* but not *OnePole/LP*; fixed after the run (the fixture is no longer unseen) · **2 · mixed drop — measured:** engine/tests/test_acceptance_mixed_drop.py · **3 · corpus — measured:** engine/tests/test_acceptance_corpus.py (three RTTI variants of the synthetic PE + a byte-identical copy → 3 jobs, independent manifests, no class-name contamination, one INFERRED family, cache reuse, USER_ARTIFACT entries beside 33 clean-room DSP references, corpus report) · **4 · source + binary — measured** on both known-source fixtures: blind recovery with the source withheld (integrity ok), then the evaluator's quantitative metrics (06_validation/known_source_metrics.json: recall/precision per category, behavioural RMSE, failure classes) · **5 · owner recovery:** blocked on the SP-emulation plugin (B-003) |
| B9 | exit criteria | reviewed in docs/hardening/B9_EXIT_CRITERIA.md: 13 of 15 measured (clean build, workers real, router extensible, mixed input, unknowns preserved, workers fail safely, jobs resume, cache, export round-trip, two known-source fixtures validate, one black-box run, unseen fixture tested, failure ≠ success); "no known blocker" = owner decisions only (B-001…B-008); the 21-plugin corpus regression stays blocked (B-001). Fingerprint stability (3.2) measured separately |
