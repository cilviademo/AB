# B9 — exit criteria review (ADDENDUM B)

Each criterion maps to the evidence that measured it. "Measured" means run here (Linux container) with the
output quoted in docs/STATUS.md or a test in `engine/tests/`. Nothing below is marked done without a run.

| # | criterion | state | evidence |
|---|---|---|---|
| 1 | clean build | measured | `app` (`npm run build`: tsc + vite + static engine), `engine` (`pytest -q` → 127 passed), `native/vst3host` (`cmake --build`); docs/hardening/BUILD_VALIDATION_REPORT.md; fresh-clone build 3 m 53 s (A7). Windows/MSVC rows pending-windows (B-005) |
| 2 | no known blocker | owner decisions only | open items in docs/BLOCKERS.md are owner decisions (B-001 corpus binaries, B-002 VST3 SDK licence, B-003 SP plugin, B-004 BTZ, B-005 Windows runs, B-006 GitHub, B-008 iPlug2 toolchain), not engineering blockers: every pipeline stage runs end-to-end on two known-source fixtures without them |
| 3 | workers real, not stubs | measured | vst3host (C++ host, runtime + probes + cross-load), Ghidra headless (RTTI / callgraph / fingerprints / decompile scripts), Node static engine (frozen v2 port), CMake + JUCE build, pluginval, VST3 SDK validator — every one runs isolated (timeout, temp cwd, scrubbed env, captured stdio, exit + crash state; engine/tests/test_workers.py) |
| 4 | router extensible | measured | one registry entry per type, capability list, no other switch (engine/tests/test_router.py::test_registry_is_the_only_switch); 44 artifact types in the registry, `ab-cli route` |
| 5 | mixed input works | measured | B8 test 2 (engine/tests/test_acceptance_mixed_drop.py): VST3 + PDB + presets + RPP + WAV + PNG + XML + source + ZIP + unknown → inventoried, routed, related, exported |
| 6 | unknowns preserved | measured | `UNKNOWN_ARTIFACT` records carry hash, size, name, mime, magic evidence and `preserve` capability; kept in the object store and the manifest (test_router, test_acceptance_mixed_drop, test_hardening_b7 adversarial drop) |
| 7 | workers fail safely | measured | timeout kills the process tree, missing program is a row, hostile inputs to vst3host fail with codes 2–5 (20 cases), a stage exception fails its stage only (engine/tests/test_workers.py, test_runtime_host.py, test_jobs.py) |
| 8 | jobs resume | measured | stage records persist per stage; a failed stage is re-runnable and everything upstream is reused by cache key (test_jobs: unchanged re-run does zero work; failure marks the stage and skips downstream; version bump re-runs only downstream); `bundle.import` restores a job with its stage records on another machine |
| 9 | cache works | measured | key = artifact · tool version · stage version · config; synthetic PE cold 1.33 s vs cache hit 0.23 s; knowledge base 100 % KNOWN on a second pass (docs/hardening/PERFORMANCE_REPORT.md, STATUS A2) |
| 10 | exports round-trip | measured | export → `ab-cli import` on a second workspace → identical job, context, stage records, cache keys, byte-identical evidence tree, identical re-export (engine/tests/test_hardening_b7.py) |
| 11 | ≥ 1 known-source fixture validates | measured | `fixtures/groundtruth` (ABGroundTruth): every phase 1–4 gate PASS on the stripped build, 0 FP / 0 FN, licensing stub classified + state fields cross-loaded; `fixtures/groundtruth_unseen` (ABUnseen): all phase 1, 2, 4 gates PASS, 0 FP / 0 FN (STATUS 1.6, 2.x, 4.x, C2, B8) |
| 12 | ≥ 1 black-box fixture runs | measured | job `ab-018943e1cc9f` (stripped ABGroundTruth) re-typed explicitly to `BLACK_BOX_REFERENCE` / `SOURCE_UNAVAILABLE` (`ab-cli reference --set-context`, history recorded), then RECONSTRUCT → BUILD → COMPARE: SURROGATE built, Waveshaper BEHAVIORALLY_EQUIVALENT, CROSS_LOAD_VALIDATED; export 322 files, 0 path / 0 secret findings, GIT_READY true, CONTEXT.md carries the black-box wording (evidence for comparison, never copied into a user project unless chosen). The run found two bugs, both fixed with tests: the job upsert used INSERT OR REPLACE and the foreign keys cascaded the job's inputs away; a project from before the current manifest contract could not be re-typed (now recorded in `00_manifest/context_history.json`) |
| 13 | 21-plugin corpus regression passes | blocked (B-001) | the four static_v2 fixture binaries and the corpus are not in the repository; descriptors, `diff-baseline` and the corpus layer are in place and measured on the synthetic fixture; B8 test 3 covers the corpus mechanics on synthetic families |
| 14 | an unseen fixture has been tested | measured | B8 test 1: ABUnseen, first blind run recorded as-is (one honest CLASSIFICATION miss, fixed afterwards and marked as such) |
| 15 | reports distinguish failure from success | measured | stage statuses OK / FAILED / SKIPPED / BLOCKED, CONTRACT_UNMET, per-module FAILED / PERCEPTUALLY_CLOSE listed with the failing renders, known-source failure classes, `LICENSE_REQUIRES_MANUAL_REVIEW` when an implementation is not reconstructed, ground-truth rows FAIL when they fail (the unseen run's P3 row) — never a false green (B3) |

Fingerprint stability across Release+symbols and stripped (EXECUTE 3.2 row): measured once the current Release+symbols
build has been decompiled against the stripped build — see STATUS 3.2.
